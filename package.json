{
  "name": "mc-assets-grabber",
  "version": "1.1.0",
  "description": "",
  "main": "error.js",
  "scripts": {
    "rip": "node ripHash.js && node ripResource.js && echo \"Finished ripping files\" && exit 0",
    "ripHash": "node ripHash.js && echo \"Finished ripping hash files\" && exit 0",
    "ripResource": "node ripResource.js && echo \"Finished ripping resource file\" && exit 0",
    "cleanup": "node cleanup.js && exit 0",
    "list": "node list.js && exit 0"
  },
  "author": "",
  "license": "BSD-3.0",
  "dependencies": {
    "node-merge-fix": "^1.2.0"
  }
}
